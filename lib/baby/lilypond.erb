\header {
  title = \markup \center-column { "Site-reading for <%= Time.now %>" }
  tagline = "Music generated by Baby::Got::Bach! (difficulty: <%= difficulty %>)"
  instrument = "<%= instrument %>"
  composer = "Baby::Got::Bach"
}

\version "2.12.2"

global = {
  \tempo 4 = <%= tempo %>
  \time <%= time_sig %>
}

<%= notes %>

